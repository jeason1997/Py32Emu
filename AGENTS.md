# Py32Emu 项目 Agent 规则

本文定义整个 Py32Emu 仓库共同遵守的项目级约定。子目录中的 `AGENTS.md` 在此基础上
补充该领域的具体规则；用户在当前任务中的最新明确要求优先。

## 规则索引

- `src/AGENTS.md`：C11 CPU、总线、芯片装配、固件加载和外设模型。
- `examples/AGENTS.md`：示例固件、SDK 工具链和可观察成功条件。
- `frontends/AGENTS.md`：前端与共享 C 核心的边界。
- `frontends/web/AGENTS.md`：Node 服务、浏览器交互和实际页面验证。
- `tests/AGENTS.md`：单元测试、真实固件、官方示例矩阵和实机验证。
- `docs/AGENTS.md`：README、进度、兼容矩阵、架构和限制说明。

`include/py32emu/` 是 `src/` 的公共接口层，修改时同时遵守 `src/AGENTS.md`。

## 交流、进度与结论

- 与用户交流、文档、提交说明和关键代码注释使用中文，先报告结果，再说明必要细节。
- 持续工作时，每次进度更新必须同时报告“整体进度百分比”和“当前阶段百分比”。
- 百分比必须注明口径。首个功能级版本完成不能写成“官方全部示例兼容 100%”。
- “某模块代表性示例通过”与“该模块全部官方示例通过”严格区分；没有逐项矩阵证据时，
  不得声称全部兼容或完整模拟。
- 不确定行为优先检查官方参考手册、数据手册、CMSIS、HAL 源码、现有测试和实物，
  不凭空猜测。资料自身存在错误时应记录证据，不为坏例程加入违反硬件语义的特例。
- 每个阶段更新 `docs/PROGRESS.md`；官方示例覆盖变化同时更新兼容矩阵。

## 总体目标与架构红线

Py32Emu 面向学习、固件验证和调试，当前完整兼容目标是 PY32F002A，未来可增加
PY32F003 等型号。实现必须模块化、可测试，不能只让某个示例“看起来能运行”。

- 公共核心使用 C11，并在 WSL/Linux 下使用 GCC 构建和测试。
- CPU、总线、芯片描述、SoC 装配、固件加载、外设模型和前端必须分层。
- 型号容量、Flash 页大小、IRQ 数量和外设集合由 `Py32ChipDescription` 决定；CPU、
  加载器和前端不得写死型号差异。
- 外设行为依据官方资料实现。示例失败时修复通用 CPU/外设语义或测试基础设施，
  禁止按 PC、符号名、固件哈希或示例名称添加特殊路径。
- CLI、Web 和测试必须复用同一个 `Py32Soc`，不得在 JavaScript 或测试脚本中重写一套 CPU。
- 不过度设计：只有第二个型号或第二种真实差异出现时，才抽取新的地址表、注册机制或抽象层。

## 构建、工具链与资料

- 日常编译和测试在 WSL 中执行，不使用 Windows 编译结果作为通过证据。
- 所有构建产物进入 `build/` 或示例自己的 `build/`；源代码目录不留临时 ELF、BIN、日志。
- 统一入口是根目录 Makefile 和 POSIX Shell 脚本；PowerShell 只用于宿主协调，不复制构建逻辑。
- 工具优先从 `PATH` 查找，不写死用户安装目录。
- 官方 SDK 默认资料位于 `E:/BaiduNetdiskDownload/Py32资料/`，WSL 路径为 `/mnt/e/...`；
  脚本若依赖该路径，必须在文档中说明。
- 可使用 DAPLink/OpenOCD 对真实 PY32F002A 进行只读检查、烧录和调试；实机结论要记录
  芯片描述、固件、命令和可观察寄存器，不能由一次现象外推所有器件。

## 测试与完成门禁

功能修改按风险执行对应验证，包括：

1. `-std=c11 -Wall -Wextra -Wpedantic` 无新增警告；
2. CPU、总线和外设单元测试；
3. `arm-none-eabi-gcc` 生成的真实 Cortex-M0+ 固件；
4. 受影响的官方 SDK 原版示例；
5. CLI、Web 后端和 HTTP 服务集成测试；
6. 涉及时序、引脚或隐藏容量时的实机对照。

阶段完成前至少执行 `git diff --check` 和受影响测试。整体完成必须从干净状态执行
`make clean && make test`，并按官方示例清单逐项证明；“未运行”“仅编译”“进入死循环但
无成功观察量”均不能计为通过。

## Git 与工作区安全

- 用户已授权本项目阶段完成后直接提交并推送；每个独立阶段使用中文提交说明并推送当前分支。
- 不自行创建或切换分支，不依赖 `gh` 完成普通 commit/push。
- 提交前确认范围、测试和 `git diff --check`，不提交 `build/` 产物。
- 工作区已有改动默认属于用户；不覆盖、删除、格式化或提交无关修改。
- 禁止使用 `git reset --hard`、`git checkout --` 等破坏性命令清理用户修改。
- 删除文件前确认属于当前任务且可恢复；测试源代码不是临时文件。

## Agent 规则维护

新增规则时放入覆盖范围最小且语义匹配的 `AGENTS.md`。仅适用于某个子系统的规则不得堆到
根文件；同一规则不在根目录和子目录重复维护。
