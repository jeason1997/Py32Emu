# 前端通用规则

- 所有前端调用共享 C11 `Py32Soc`，不得在 UI 层复制 CPU、寄存器或外设状态机。
- 前端只展示和注入状态；协议、计时、IRQ 和外设结果由 C 模型产生。
- 型号选择和可见外设服从芯片描述能力位，不在页面写死 PY32F002A 的全部实例。
- 高频运行时对状态更新节流，避免刷新频率改变模拟时间或造成界面卡顿。
- 新增前端能力时同步更新协议测试、用户文档和错误处理。
