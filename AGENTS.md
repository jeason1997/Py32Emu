# Py32Emu 项目约定

Py32Emu 是面向学习和固件验证的 PY32 Cortex-M0+ 模拟器。当前目标型号是
PY32F002A，架构必须允许后续增加 PY32F003 等型号，但不提前实现尚未需要的抽象。

- 与用户交流、文档和关键代码注释使用中文。
- 公共核心使用 C11，CPU、总线、芯片描述、外设、固件加载、前端分层。
- 型号容量、地址和外设集合只能放在芯片描述中，不得散落在 CPU 或前端。
- 外设行为以官方参考手册、数据手册和固件库为依据；不为单个例程硬编码行为。
- 所有构建产物放入 `build/`；功能改动应增加相应测试。
- 每轮工作更新 `docs/PROGRESS.md`，记录已完成、验证结果、下一步和已知问题。
- 未经用户明确要求，不创建分支、不提交、不推送。

